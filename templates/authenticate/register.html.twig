{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}

<div class="w-full flex justify-center">
    <div class="w-11/12">
        <h1 class="h3 mb-3 font-weight-normal text-center font-bold underline my-8">Inscription Utilisateur :</h1>
        {{ form_start(form, {'attr': {'class': 'bg-gray-300 p-12 rounded-md'}}) }}
            {% if errorsForm is defined %}
                {% for error in errorsForm %}
                    <div class="flex justify-center items-center my-2 w-full">
                        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative w-1/2" role="alert">
                            <strong class="font-bold">Erreur : </strong>
                            <span class="block sm:inline">{{ error }}</span>
                        </div>
                    </div>
                {% endfor %}
            {% endif %}
            <div class="mb-6">
                {{ form_label(form.email, 'Votre adresse mail :', {'label_attr': {'class': 'block mb-2 text-sm underline font-medium text-gray-900'}}) }}
                {{ form_widget(form.email, {'attr': {'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5', 'placeholder': 'example@mail.com', 'required': 'required'}}) }}
            </div>
            <div class="mb-6">
                {{ form_label(form.name, 'Votre Nom :', {'label_attr': {'class': 'block mb-2 text-sm underline font-medium text-gray-900'}}) }}
                {{ form_widget(form.name, {'attr': {'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5', 'placeholder': 'name', 'required': 'required'}}) }}
            </div>
            <div class="mb-6">
                {{ form_label(form.password, 'Votre mot de passe :', {'label_attr': {'class': 'block mb-2 text-sm underline font-medium text-gray-900'}}) }}
                {{ form_widget(form.password, {'attr': {'class': 'bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5', 'placeholder': '**********', 'autocomplete': 'new-password', 'required': 'required'}}) }}
            </div>
            <div class="flex justify-start items-center w-fit-content gap-2 mb-4">
                {{ form_label(form.agreeTerms, 'Accepter les conditions pour vous inscrire :', {'label_attr': {'class': 'block mb-1 text-sm font-medium text-gray-900'}}) }}
                {{ form_widget(form.agreeTerms, {'attr': {'class': 'cursor-pointer bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block', 'required': 'required'}}) }}
            </div>
            <div class="flex justify-center w-full">
                <input type="submit" class="cursor-pointer text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm w-full sm:w-auto px-5 py-2.5 text-center" value="S'inscrire"/>
            </div>
        {{ form_end(form) }}
   </div>
</div>
{% endblock %}
